# docker-infrastructure

### new-host
To be done on all new hosts before anything else

### runner
To be ran on the host that will contain the github runner

#### notes
When you add a new host be sure to run the new-host script as well as registering the ssh with the runner